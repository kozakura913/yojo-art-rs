[package]
name = "e2e-test"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.9"
reqwest = { version = "0.12", default-features = false , features = ["rustls-tls-native-roots", "blocking"] }
serde_json = "1"
